---
title: 'Pagination'
description: 'The Pagination component allows you to display active page and navigate between multiple pages.'
url: https://nextui.com/docs/components/pagination
---

# Pagination

The Pagination component allows you to display active page and navigate between multiple pages.

```jsx
import { Pagination } from '@nextui-org/react';
```

<CarbonAd />

<Playground
  title="Default"
  desc="You can set a starting page with the `initialPage` prop."
  code={`
 <Pagination total={20} initialPage={1} />
`}
/>

<Playground
  title="Colors"
  desc="You can change the color of the `Pagination` with the property `color`."
  code={`
() => {
const [selectedColor, setSelectedColor] = React.useState('primary');
const colors = ['primary', 'secondary', 'success', 'warning', 'error', '#f4a']
const capitalize = (str) => {
  const lower = str.toLowerCase();
  return str.charAt(0).toUpperCase() + lower.slice(1);
}
return(
<Grid.Container gap={2}>
    <Grid xs={12}>
      <Pagination color={selectedColor} total={10} />
    </Grid>
    <Grid xs={12}>
        <Radio.Group row size="small" value={selectedColor} onChange={setSelectedColor}>
            {colors.map((color) => (
                <Radio key={color} value={color} color={color}>
                    {capitalize(color)}
                </Radio>
            ))}
        </Radio.Group>
    </Grid>    
</Grid.Container>
)}  
`}
/>

<Playground
  title="Sizes"
  desc="Change the size of the entire `Pagination` including `width`, `height` and `font-size` with the `size` property."
  code={`
() => {
const [selectedSize, setSelectedSize] = React.useState('medium');
const sizes = ['mini', 'small', 'medium', 'large', 'xlarge']
const capitalize = (str) => {
  const lower = str.toLowerCase();
  return str.charAt(0).toUpperCase() + lower.slice(1);
}
return(
<Grid.Container gap={2}>
    <Grid xs={12}>
      <Pagination color='secondary' size={selectedSize} total={10} />
    </Grid>
    <Grid xs={12}>
        <Radio.Group row size="small" value={selectedSize} onChange={setSelectedSize}>
            {sizes.map((size) => (
                <Radio color="secondary" key={size} value={size}>
                    {capitalize(size)}
                </Radio>
            ))}
        </Radio.Group>
    </Grid>    
</Grid.Container>
)}  
`}
/>

<Playground
  title="Rounded"
  desc="You can completely round the corners of any type of `Pagination` with the `rounded` property."
  code={`
<Pagination rounded  total={10} initialPage={6} />
`}
/>

<Playground
  title="Bordered"
  desc="You can set a starting page with the `initialPage` prop."
  code={`
<Grid.Container xs={12} gap={2} >  
    <Grid>
        <Pagination bordered total={10} initialPage={6} />
    </Grid>
    <Grid>
        <Pagination rounded bordered total={10} initialPage={6} />
    </Grid>
</Grid.Container>
`}
/>

<Playground
  title="Shadow"
  desc="You can set a starting page with the `initialPage` prop."
  code={`
() => {
const [selectedColor, setSelectedColor] = React.useState('primary');
const colors = ['primary', 'secondary', 'success', 'warning', 'error', '#f4a']
const capitalize = (str) => {
  const lower = str.toLowerCase();
  return str.charAt(0).toUpperCase() + lower.slice(1);
}
return(
<Grid.Container gap={2}>
    <Grid xs={12}>
      <Pagination shadow color={selectedColor} total={10} />
    </Grid>
    <Grid xs={12}>
        <Radio.Group row size="small" value={selectedColor} onChange={setSelectedColor}>
            {colors.map((color) => (
                <Radio key={color} value={color} color={color}>
                    {capitalize(color)}
                </Radio>
            ))}
        </Radio.Group>
    </Grid>    
</Grid.Container>
)}  
`}
/>

<Playground
  title="Only Dots"
  desc="You can completely change the style of the component and remove the internal numbers in each `Pagination` button with the property `onlyDots`."
  code={`
() => {
const [selectedSize, setSelectedSize] = React.useState('medium');
const sizes = ['mini', 'small', 'medium', 'large', 'xlarge']
const capitalize = (str) => {
  const lower = str.toLowerCase();
  return str.charAt(0).toUpperCase() + lower.slice(1);
}
return(
<Grid.Container gap={2}>
    <Grid xs={12}>
      <Pagination onlyDots size={selectedSize} total={6} />
    </Grid>
    <Grid xs={12}>
        <Radio.Group row size="small" value={selectedSize} onChange={setSelectedSize}>
            {sizes.map((size) => (
                <Radio key={size} value={size}>
                    {capitalize(size)}
                </Radio>
            ))}
        </Radio.Group>
    </Grid>    
</Grid.Container>
)}  
`}
/>

<Playground
  title="Loop"
  desc="You can make the pagination infinite with the `loop` property, this means that 
  the arrow buttons are no longer in the disabled state."
  code={`
<Pagination loop color="success" total={10} initialPage={6} />
`}
/>

<Playground
  title="No Margin"
  desc="You can change the style of the entire `Pagination` component by removing the 
  margin between the buttons and changing the radius with the `noMargin` property."
  code={`
<Pagination noMargin shadow total={10} initialPage={6} />
`}
/>

<Playground
  title="No Controls"
  desc="You can remove the `next` and `previous` arrows with `controls={false}` property."
  code={`
<Grid.Container gap={2}> 
    <Grid> 
        <Pagination shadow controls={false} total={10} initialPage={6} />
    </Grid>
    <Grid>
        <Pagination noMargin shadow controls={false} total={10} initialPage={6} />
    </Grid>
</Grid.Container>
`}
/>

<Playground
  title="No Animated"
  desc="You can disable the animation of the entire `Modal` with the property `animated={false}`."
  code={`
<Pagination animated={false} total={10} initialPage={6} />
`}
/>

<Playground
  title="Siblings"
  desc="You can control the amount of active item siblings with `siblings` prop."
  code={`
<Grid.Container xs={12} gap={2}>  
    <Grid>
        <Text>1 Sibling (default)</Text>
        <Spacer y={0.4}/>
        <Pagination bordered total={14} initialPage={6} />
    </Grid>
    <Grid>
        <Text>2 Siblings</Text>
        <Spacer y={0.4}/>
        <Pagination bordered siblings={2} total={14} initialPage={6} />
    </Grid>
    <Grid>
        <Text>3 Siblings</Text>
        <Spacer y={0.4}/>
        <Pagination bordered siblings={3} total={14} initialPage={6} />
    </Grid>    
</Grid.Container>
`}
/>

<Playground
  title="Boundaries"
  desc="You can control the amount of items displayed after previous and before next buttons with `boundaries` prop."
  code={`
<Grid.Container xs={12} gap={2}>  
    <Grid>
        <Text>1 Boundary (default)</Text>
        <Spacer y={0.4}/>
        <Pagination bordered total={14} initialPage={6} />
    </Grid>
    <Grid>
        <Text>2 Boundaries</Text>
        <Spacer y={0.4}/>
        <Pagination bordered boundaries={2} total={14} initialPage={6} />
    </Grid>
    <Grid>
        <Text>3 Boundaries</Text>
        <Spacer y={0.4}/>
        <Pagination bordered boundaries={3} total={14} initialPage={6} />
    </Grid>    
</Grid.Container>
`}
/>

<Spacer y={3} />

### APIs

<Spacer y={2} />

#### Pagination Props

| Attribute        | Type                     | Accepted values                  | Description                                                      | Default   |
| ---------------- | ------------------------ | -------------------------------- | ---------------------------------------------------------------- | --------- |
| **color**        | `NormalColors` `string`  | [NormalColors](#normal-colors)   | Change pagination color                                          | `default` |
| **size**         | `NormalSizes` `number`   | [NormalSizes](#normal-sizes)     | Change pagination size                                           | `medium`  |
| **page**         | `number`                 | -                                | Active page number                                               | -         |
| **initialPage**  | `number`                 | -                                | Selected page on initial render                                  | `1`       |
| **total**        | `number`                 | -                                | Total amount of pages                                            | `1`       |
| **siblings**     | `number`                 | -                                | Amount on left/right side of selected page                       | `1`       |
| **boundaries**   | `number`                 | -                                | Amount of elements visible on left/right edges                   | `1`       |
| **dotsJump**     | `number`                 | -                                | Number of pages that are added or subtracted on the '...' button | `5`       |
| **bordered**     | `boolean`                | `true/false`                     | Bodered pagination                                               | `false`   |
| **rounded**      | `boolean`                | `true/false`                     | Rounded pagination                                               | `false`   |
| **shadow**       | `boolean`                | `true/false`                     | Display shadow effect                                            | `false`   |
| **animated**     | `boolean`                | `true/false`                     | Display move and scale animation                                 | `true`    |
| **borderWeight** | `NormalWeights`          | [NormalWeights](#normal-weights) | Border weight for `bordered` pagination                          | `normal`  |
| **loop**         | `boolean`                | `true/false`                     | Non disable next/previous controls                               | `false`   |
| **noMargin**     | `boolean`                | `true/false`                     | Remove margin amongst pagination items                           | `false`   |
| **controls**     | `boolean`                | `true/false`                     | Enable/Disable pagination controls                               | `true`    |
| **onlyDots**     | `boolean`                | `true/false`                     | Show only dots as pagination elements                            | `false`   |
| **onChange**     | `(page: number) => void` | -                                | Callback fired after change of each page                         | -         |
| ...              | `HTMLAttributes`         | `'id', 'className', ...`         | Native props                                                     | -         |

### Pagination types

<Spacer y={1} />

#### Normal Colors

```ts
type NormalColors =
  | 'default'
  | 'primary'
  | 'secondary'
  | 'success'
  | 'warning'
  | 'error'
  | 'gradient';
```

#### Normal Sizes

```ts
type NormalSizes = 'mini' | 'small' | 'medium' | 'large' | 'xlarge';
```

#### Normal Weights

```ts
type NormalWeights = 'light' | 'normal' | 'bold';
```
