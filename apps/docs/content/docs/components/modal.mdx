---
title: 'Modal'
description: 'Display a dialog with a custom content that requires attention or provides additional information.'
url: https://v1.nextui.org/docs/components/modal
---

# Modal

Displays a dialog with a custom content that requires attention or provides additional information.

```jsx
import { Modal } from '@nextui-org/react';
```

<CarbonAd />

<Spacer y={2} />

<Playground
  title="Default"
  desc="Use `open` prop to control whether Modal is displayed."
  files={modalContent.default_modal}
/>

<Playground
  title="Large content"
  desc="There are cases where you need a scroll because there is a lot of information
   within the `Modal` for this you can use the `scroll` property."
  files={modalContent.largeContent}
/>

<Playground
  title="Full screen"
  desc="If you need the `Modal` to be the total window size you can do it with the `fullScreen` property."
  files={modalContent.fullscreen}
/>

<Playground
  title="Without padding"
  desc="If you need to remove the padding from the dialog to make a more 
  personalized interface you can do it with the `noPadding` property."
  files={modalContent.withoutPadding}
/>

<Playground
  title="Blur background"
  desc="You can add a `blur` style to all the elements behind the `Modal` with the `blur` property,
this functionality depends on the css property [backdrop-filter](https://caniuse.com/css-backdrop-filter)."
  files={modalContent.blurBackground}
/>

<Playground
  title="Prevent close"
  desc="With the `preventClose` property you can't close the dialog by clicking outside or pressing the `esc` key."
  files={modalContent.preventClose}
/>

<Playground
  title="No Animated"
  desc="You can disable the animation of the entire `Modal` with the property `animated={false}`."
  files={modalContent.noAnimated}
/>

<Spacer y={3} />

### APIs

<Spacer y={2} />

#### Modal Props

| Attribute        | Type                          | Accepted values          | Description                                                                                   | Default   |
| ---------------- | ----------------------------- | ------------------------ | --------------------------------------------------------------------------------------------- | --------- |
| **open**         | `boolean`                     | `true/false`             | Determine if the modal is visible or hidden                                                   | `false`   |
| **scroll**       | `boolean`                     | `true/false`             | Makes the content a maximum high and gives the possibility to overflow the content add scroll | `false`   |
| **blur**         | `boolean`                     | `true/false`             | Makes all elements blur when the modal opens                                                  | `false`   |
| **animated**     | `boolean`                     | `true/false`             | Display the modal with opening and close animation                                            | `true`    |
| **noPadding**    | `boolean`                     | `true/false`             | Eliminates the padding of the base elements of the modal                                      | `false`   |
| **closeButton**  | `boolean`                     | `true/false`             | Determine if the modal close button is visible or hidden                                      | `false`   |
| **fullScreen**   | `boolean`                     | `true/false`             | Makes the modal the size of the window                                                        | `false`   |
| **autoMargin**   | `boolean`                     | `true/false`             | Apply or not a margin to the base elements of the modal                                       | -         |
| **preventClose** | `boolean`                     | `true/false`             | Modal cannot be closed by clicking outside or by pressing the esc key                         | `false`   |
| **width**        | `string` `number`             | `%` `px` `rem` `em`      | Determine the width of the modal                                                              | `400px`   |
| **onClose**      | `() => void`                  | -                        | Callback fired when the modal is closed                                                       | -         |
| **onOpen**       | `() => void`                  | -                        | Callback fired when the modal is open                                                         | -         |
| **css**          | `Stitches.CSS`                | -                        | Override Default CSS style                                                                    | -         |
| **as**           | `keyof JSX.IntrinsicElements` | -                        | Changes which tag component outputs                                                           | `section` |
| ...              | `HTMLAttributes`              | `'id', 'className', ...` | Native props                                                                                  | -         |

<Spacer y={2} />

#### Modal.Header Props

| Attribute      | Type                          | Accepted values          | Description                                             | Default  |
| -------------- | ----------------------------- | ------------------------ | ------------------------------------------------------- | -------- |
| **autoMargin** | `boolean`                     | `true/false`             | Apply or not a margin to the base elements of the modal | `true`   |
| **justify**    | `Justify`                     | [Justify](#justify)      | CSS "justify-content" property                          | `center` |
| **css**        | `Stitches.CSS`                | -                        | Override Default CSS style                              | -        |
| **as**         | `keyof JSX.IntrinsicElements` | -                        | Changes which tag component outputs                     | `div`    |
| ...            | `HTMLAttributes`              | `'id', 'className', ...` | Native props                                            | -        |

<Spacer y={2} />

#### Modal.Body Props

| Attribute      | Type                          | Accepted values          | Description                                             | Default |
| -------------- | ----------------------------- | ------------------------ | ------------------------------------------------------- | ------- |
| **autoMargin** | `boolean`                     | `true/false`             | Apply or not a margin to the base elements of the modal | `true`  |
| **css**        | `Stitches.CSS`                | -                        | Override Default CSS style                              | -       |
| **as**         | `keyof JSX.IntrinsicElements` | -                        | Changes which tag component outputs                     | `div`   |
| ...            | `HTMLAttributes`              | `'id', 'className', ...` | Native props                                            | -       |

<Spacer y={2} />

#### Modal.Footer Props

| Attribute      | Type                          | Accepted values          | Description                                             | Default    |
| -------------- | ----------------------------- | ------------------------ | ------------------------------------------------------- | ---------- |
| **autoMargin** | `boolean`                     | `true/false`             | Apply or not a margin to the base elements of the modal | `true`     |
| **justify**    | `Justify`                     | [Justify](#justify)      | CSS "justify-content" property                          | `flex-end` |
| **css**        | `Stitches.CSS`                | -                        | Override Default CSS style                              | -          |
| **as**         | `keyof JSX.IntrinsicElements` | -                        | Changes which tag component outputs                     | `div`      |
| ...            | `HTMLAttributes`              | `'id', 'className', ...` | Native props                                            | -          |

<Spacer y={2} />

### Modal types

<Spacer y={1} />

#### Justify

```ts
type Justify =
  | 'flex-start'
  | 'center'
  | 'flex-end'
  | 'space-between'
  | 'space-around'
  | 'space-evenly';
```

#### useModal

```ts
type useModal = (initialVisible: boolean) => {
  visible: boolean;
  setVisible: Dispatch<SetStateAction<boolean>>;
  currentRef: MutableRefObject<boolean>;
  bindings: {
    open: boolean;
    onClose: () => void;
  };
};
```
