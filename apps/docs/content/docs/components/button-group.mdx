---
title: 'Button Group'
description: 'If you need to make a group of buttons you can use the compound component `Button.Group` and inside the buttons you want to group.'
url: 'https://nextui.org/docs/components/button-group'
---

# Button Group

If you need to make a group of buttons you can use the compound component `Button.Group` and
inside the buttons you want to group.

```jsx
import { Button } from '@nextui-org/react';
```

<CarbonAd />

<Spacer y={2} />

<Playground
  title="Default"
  desc="The default `Button.Group`"
  files={buttonGrpContent.default_grp}
/>

<Playground
  title="Disabled"
  desc="You can disabled all buttons with the property `disabled`."
   files={buttonGrpContent.disabledGrp}
/>

<Playground
  title="Loading"
  desc="You can use the loading status in any button you want"
     files={buttonGrpContent.loading}
/>

<Playground
  title="Sizes"
  desc="Change the size of the entire group button including `padding`, `font-size` and `border` with the size property."
  files={buttonGrpContent.sizes}
/>

<Playground
  title="Vertical"
  desc="You can use the `vertical` property to sort the buttons vertically."
  files={buttonGrpContent.vertical}
/>

<Playground
  title="Variants"
  desc="You can use any `Button` variant in a `Button.Group`"
 files={buttonGrpContent.variants}
/>

<Spacer y={3} />

### APIs

<Spacer y={2} />

#### Button Group Props

| Attribute        | Type                              | Accepted values                  | Description                            | Default        |
| ---------------- | --------------------------------- | -------------------------------- | -------------------------------------- | -------------- |
| **color**        | `NormalColors`                    | [NormalColors](#normal-colors)   | Change buttons color                   | `default`      |
| **size**         | `NormalSizes`                     | [NormalSizes](#normal-sizes)     | Change buttons size                    | `medium`       |
| **disabled**     | `boolean`                         | `true/false`                     | Disable all buttons                    | `false`        |
| **bordered**     | `boolean`                         | `true/false`                     | Bordered buttons                       | `false`        |
| **flat**         | `boolean`                         | `true/false`                     | Flat buttons                           | `false`        |
| **rounded**      | `boolean`                         | `true/false`                     | Rounded buttons                        | `false`        |
| **ghost**        | `boolean`                         | `true/false`                     | The opposite color                     | `false`        |
| **auto**         | `boolean`                         | `true/false`                     | Autoscale buttons width                | `false`        |
| **ripple**       | `boolean`                         | `true/false`                     | Display drip animation for all buttons | `true`         |
| **animated**     | `boolean`                         | `true/false`                     | Display drip and scale animation       | `true`         |
| **borderWeight** | `NormalWeights`                   | [NormalWeights](#normal-weights) | Border weight for `bordered` buttons   | `normal`       |
| **css**          | `Stitches.CSS`                    | -                                | Override Default CSS style             | -              |
| **as**           | `keyof JSX.IntrinsicElements`     | -                                | Changes which tag component outputs    | `div`          |
| ...              | `ButtonHTMLAttributes`            | `'id', 'className', ...`         | Native button props                    | -              |

<Spacer y={2} />

### Button group types

<Spacer y={1} />

#### Normal Colors

```ts
type NormalColors =
  | 'default'
  | 'primary'
  | 'secondary'
  | 'success'
  | 'warning'
  | 'error'
  | 'gradient';
```

#### Normal Sizes

```ts
type NormalSizes = 'xs' | 'sm' | 'md' | 'lg' | 'xl';
```

#### Normal Weights

```ts
type NormalWeights = 'light' | 'normal' | 'bold' | 'extrabold' | 'black';
```
