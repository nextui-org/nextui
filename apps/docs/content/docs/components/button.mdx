---
title: 'Button'
description: 'Buttons allow users to perform actions and choose with a single tap.'
url: https://nextui.org/docs/components/button
---

# Button

Buttons allow users to perform actions and choose with a single tap.

```jsx
import { Button } from '@nextui-org/react';
```

<CarbonAd />

<Spacer y={2} />

<Playground
  title="Default"
  desc="The default `Button` contains an animation effect."
  files={buttonContent.default_button}
/>

<Playground
  title="Disabled"
  desc="Unusable and un-clickable `Button`."
  files={buttonContent.disabled_button}
/>

<Playground
  title="Sizes"
  desc="Change the size of the entire `Button` including `padding`, `font-size` and `border` with the `size` property."
  files={buttonContent.sizes}
/>

<Playground
  title="Colors"
  desc="You can change the color of the `Button` with the property `color`."
  files={buttonContent.color}
/>

<Playground
  title="Shadow"
  desc="You can add a shadow effect with the property `shadow`."
  files={buttonContent.shadow}
/>

<Playground
  title="Loading"
  desc="You can add a `loading` status with the loading property. You can change the `loader` with the `loaderType` property"
  files={buttonContent.loading}
/>

<Playground
  title="Bordered"
  desc="You can change the full style to a bordered `Button` with the `bordered` property."
  files={buttonContent.bordered}
/>

<Playground
  title="Rounded"
  desc="You can completely round the corners of any type of `Button` with the `rounded` property."
  files={buttonContent.rounded}
/>

<Playground
  title="Ghost"
  desc="You can change the full style to a ghost button with the `ghost` property."
  files={buttonContent.ghost}
/>

<Playground
  title="Flat"
  desc="You can change the full style to a flat button with the `flat` property.
                                        
**_Note: `gradient` variant is not supported with the `flat` mode._**"
  files={buttonContent.flat}
/>

<Playground
  title="Light"
  desc="You can change the full style to a light button with the `light` property. 
  
  ***_Note: `gradient` variant is not supported with the `light` mode._***"
  files={buttonContent.light}
/>

<Playground
  title="Icons"
  desc="NextUI doesn't use any library or icon font by default, with this we give the freedom to use the one you prefer. In the following example we use [React Iconly](https://react-iconly.jrgarciadev.com)"
  files={buttonContent.icon}
/>

<Spacer y={3} />

### APIs

<Spacer y={2} />

#### Button Props

| Attribute        | Type                                            | Accepted values                  | Description                               | Default   |
| ---------------- | ----------------------------------------------- | -------------------------------- | ----------------------------------------- | --------- |
| **color**        | `NormalColors`                                  | [NormalColors](#normal-colors)   | Change button color                       | `default` |
| **size**         | `NormalSizes`                                   | [NormalSizes](#normal-sizes)     | Change button size                        | `medium`  |
| **disabled**     | `boolean`                                       | `true/false`                     | Disable button                            | `false`   |
| **bordered**     | `boolean`                                       | `true/false`                     | Bordered button                           | `false`   |
| **flat**         | `boolean`                                       | `true/false`                     | Flat button                               | `false`   |
| **rounded**      | `boolean`                                       | `true/false`                     | Rounded button                            | `false`   |
| **ghost**        | `boolean`                                       | `true/false`                     | The opposite color                        | `false`   |
| **shadow**       | `boolean`                                       | `true/false`                     | Display shadow effect                     | `false`   |
| **ripple**       | `boolean`                                       | `true/false`                     | Display drip animation                    | `true`    |
| **auto**         | `boolean`                                       | `true/false`                     | Autoscale button width                    | `false`   |
| **animated**     | `boolean`                                       | `true/false`                     | Display drip and scale animation          | `true`    |
| **borderWeight** | `NormalWeights`                                 | [NormalWeights](#normal-weights) | Border weight for `bordered` button       | `normal`  |
| **autoFocus**    | `boolean`                                       | `true/false`                     | Automatically focus button                | `false`   |
| **onClick**      | `MouseEventHandler`                             | -                                | Button click handler                      | -         |
| **icon**         | `ReactNode`                                     | -                                | Show icon in button                       | -         |
| **iconRight**    | `ReactNode`                                     | -                                | Show icon on the other side of the button | -         |
| **type**         | `ButtonHTMLAttributes.type`                     | `button` `reset` `submit`        | Native type of button element             | `button`  |
| **ref**          | <Code>Ref<HTMLButtonElement &#124; null></Code> | -                                | forwardRef                                | -         |
| **css**          | `Stitches.CSS`                                  | -                                | Override Default CSS style                | -         |
| **as**           | `keyof JSX.IntrinsicElements`                   | -                                | Changes which tag component outputs       | `button`  |
| ...              | `ButtonHTMLAttributes`                          | `'id', 'className', ...`         | Button native props                       | -         |

<Spacer y={2} />

### Button types

<Spacer y={1} />

#### Normal Colors

```ts
type NormalColors =
  | 'default'
  | 'primary'
  | 'secondary'
  | 'success'
  | 'warning'
  | 'error'
  | 'gradient';
```

#### Normal Sizes

```ts
type NormalSizes = 'xs' | 'sm' | 'md' | 'lg' | 'xl';
```

#### Normal Weights

```ts
type NormalWeights = 'light' | 'normal' | 'bold' | 'extrabold' | 'black';
```
