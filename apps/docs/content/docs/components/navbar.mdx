---
title: "Navbar"
description: "Navbar"
url: https://nextui.org/docs/components/badge
---

# Navbar

Badges are used as a small numerical value or status descriptor for UI elements.

```jsx
import { Navbar } from "@nextui-org/react";
```

<CarbonAd />

<Spacer y={2} />

<Playground
  asIframe
  title="Static"
  iframeSrc="/examples/navbar/static"
  desc="Badge with default styles, used in standalone when `content` prop is empty."
  files={navbarContent.staticVariant}
/>

<Playground
  asIframe
  title="Sticky"
  iframeSrc="/examples/navbar/sticky"
  desc="Badge with default styles, used in standalone when `content` prop is empty."
  files={navbarContent.stickyVariant}
/>

<Playground
  asIframe
  title="Floating"
  iframeSrc="/examples/navbar/floating"
  desc="Badge with default styles, used in standalone when `content` prop is empty."
  files={navbarContent.floatingVariant}
/>
