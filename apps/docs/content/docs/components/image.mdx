---
title: 'Image'
description: 'The Image component is used to display images'
url: https://nextui.org/docs/components/image
---

# Image

The Image component is used to display images.

```jsx
import { Image } from '@nextui-org/react';
```

<CarbonAd />

<Spacer y={2} />

<Playground
  title="Default"
  desc="The default `Image` acts as img component."
  files={imageContent.default_image}
/>

<Playground
  title="Size"
  desc="Set the size of image by using the `width` & `height` props."
  files={imageContent.size}
/>

<Playground
  title="ShowSkeleton"
  desc="Shows a skeleton while the image is loading."
  files={imageContent.showSkeleton}
/>

<Playground
  title="Object-Fit"
  desc="This property tells the content to fill the container in a variety of ways. "
  files={imageContent.objectFit}
/>

<Playground
  title="Next.js Image"
  desc="If you are using Next.js you can use `next/image` to change the rendering component. "
  files={imageContent.nextjsImage}
/>

<Spacer y={3} />

### APIs

<Spacer y={2} />

#### Image Props

| Attribute        | Type                           | Description                                         | Default      |
| ---------------- | ------------------------------ | --------------------------------------------------- | ------------ |
| **src**          | `string`                       | Image source (local or remote)                      | -            |
| **autoResize**   | `boolean`                      | Resize Image to fits screen                         | `false`      |
| **showSkeleton** | `boolean`                      | Shows loading Skeleton while image is loading       | `true`       |
| **objectFit**    | [ObjectFit](#image-object-fit) | Property tells the content to fill the container    | `scale-down` |
| **width**        | `string`                       | Specifies Image Width                               | -            |
| **height**       | `string`                       | Specifies Image Height                              | -            |
| **maxDelay**     | `number`                       | Specifies how long Image Skeleton Renders Animation | `3000`       |
| **className**    | `string`                       | Give Image A ClasName                               | -            |
| **containerCSS** | `Stitches.CSS`                 | Override Default Image Contianer Style              | -            |
| **css**          | `Stitches.CSS`                 | Override Default CSS style                          | -            |
| **as**           | `keyof JSX.IntrinsicElements`  | Changes which tag component outputs                 | `img`        |
| ...              | `ImageHTMLAttributes`          | Native props                                        | -            |

<Spacer y={2} />

### Image types

<Spacer y={1} />

#### Image object fit

```ts
type ObjectFit =
   | 'contain'
   | 'cover'
   | 'fill'
   | 'none'
   | 'scale-down'
   | 'inherit'
   | 'initial'
   | 'revert'
   | 'unset'
);
```
