import App from "./multiple-selection.raw.jsx?raw";
import AppTs from "./multiple-selection.raw.tsx?raw";

const react = {
  "/App.jsx": App,
};

const reactTs = {
  "/App.tsx": AppTs,
};

export default {
  ...react,
  ...reactTs,
};
