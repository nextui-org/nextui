const App = `import { Button } from "@nextui-org/react";

export default function App() {
  return (
    <Button>
    Default
    </Button>
    );
}
`;

const react = {
  '/App.js': App
};

export default {
  ...react
};
