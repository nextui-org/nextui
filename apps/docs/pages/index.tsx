import {DefaultLayout} from "@/layouts";
import {Hero} from "@/components";

const IndexPage = () => {
  return (
    <DefaultLayout>
      <Hero />
    </DefaultLayout>
  );
};

export default IndexPage;
